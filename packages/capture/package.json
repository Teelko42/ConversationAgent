{
  "name": "@aizen/capture",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Lane B — capture source -> AudioFrame, published to the bus. Phase-0 MockClipSource (BD-03).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": { "build": "tsc -b" },
  "dependencies": {
    "@aizen/contracts": "workspace:*",
    "@aizen/edge-gateway": "workspace:*"
  }
}
