{
  "name": "@aizen/accounts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Account system — AuthProvider seam (Stub + Google/Microsoft Entra OAuth, PKCE) behind BD-03 key-gating, a swappable AccountRepository (in-memory + node:sqlite + PostgreSQL), tier-keyed entitlements, and fail-closed per-account resource quota.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -b",
    "migrate:pg": "tsx scripts/migrate-sqlite-to-postgres.ts"
  },
  "dependencies": {
    "@aizen/contracts": "workspace:*",
    "pg": "^8.21.0"
  },
  "devDependencies": {
    "@types/pg": "^8.20.0"
  }
}
