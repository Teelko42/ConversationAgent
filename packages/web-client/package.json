{
  "name": "@aizen/web-client",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Lane E — the bare Phase-0 client: a headless renderer of the per-session stream (transcript + concept cards + insights).",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": { "build": "tsc -b" },
  "dependencies": {
    "@aizen/contracts": "workspace:*",
    "@aizen/edge-gateway": "workspace:*"
  }
}
